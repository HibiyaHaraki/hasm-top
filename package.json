{
  "name": "hasm-top",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync:logo": "node scripts/sync-logo.mjs",
    "sync:markdown-design": "node scripts/sync-markdown-design.mjs",
    "sync:downloads": "node scripts/stage-downloads.mjs",
    "sync": "npm run sync:logo && npm run sync:markdown-design && npm run sync:downloads",
    "install:hasm": "npm ci --prefix submodules/hasm",
    "install:hasm-markdown": "npm ci --prefix submodules/hasm_markdown",
    "install:submodules": "npm run install:hasm-markdown && npm run install:hasm",
    "build:site": "vite build",
    "build:hasm": "npm --prefix submodules/hasm run sync:tauri-icons && npm --prefix submodules/hasm run build && npm --prefix submodules/hasm run tauri -- build --config ../../config/hasm-tauri.distribution.conf.json",
    "build:hasm-markdown": "npm --prefix submodules/hasm_markdown run tauri -- build",
    "build:installers": "npm run install:submodules && npm run build:hasm-markdown && npm run build:hasm",
    "build:all": "npm run build",
    "predev": "npm run sync",
    "dev": "vite",
    "prebuild": "npm run sync:logo && npm run sync:markdown-design",
    "build": "npm run build:installers && npm run sync:downloads && npm run build:site",
    "check:site": "node scripts/check-site.mjs",
    "lint": "oxlint",
    "preview": "vite preview"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "framer-motion": "^13.3.0",
    "react": "^19.3.0",
    "react-bootstrap": "^2.10.10",
    "react-dom": "^19.3.0",
    "react-router-dom": "^7.18.4",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^6.1.1",
    "oxlint": "^1.83.0",
    "vite": "^8.3.0"
  }
}
